package com.hostelcomplaintresolver.backend.model;

public enum Priority {
    HIGH,    // Stored as 0 in the database
    MEDIUM,  // Stored as 1 in the database
    LOW      // Stored as 2 in the database
}
